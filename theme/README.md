# Requirements

 - Sass
 - Pelican plugins: Neighbours and Assets
 - Configure in pelicanconf.py: `ICONS_PATH`, `PROFILE_ICON`.  `PROFILE_ICON` is the path within
   `ICONS_PATH` but there are other names hard-coded in the theme for favicon etc.

Inspiration from https://github.com/iKevinY/pneumatic
